import UIKit
// подходящая погода для рыбалки
let temperature = 27
let rain = false
let sun = true
let weather = temperature <= 17 && rain == false && sun == true

//подходящая погода для прогулки по парку
let wint = false
let sunny = false
let timeOfDay = "night"
let weather2 = wint == false && sunny == false && timeOfDay == "night"




